{
  "name": "@ankiniki/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for Ankiniki",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "jest": "^29.6.2",
    "typescript": "^5.1.6",
    "vitest": "^4.1.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
